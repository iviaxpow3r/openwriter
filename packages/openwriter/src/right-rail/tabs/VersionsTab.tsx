import Placeholder from './Placeholder';
import type { RightRailTabProps } from '../types';

export default function VersionsTab(_props: RightRailTabProps) {
  return <Placeholder label="Versions" note="Version history will move here." />;
}
